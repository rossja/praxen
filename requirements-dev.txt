# Build-only dependencies — NOT used by the shipped Praxen skill (which is
# stdlib-only and requires no pip install). These are needed only to regenerate
# the GitHub Pages site locally / at release time.
#
#   pip install -r requirements-dev.txt
#
# Used by docs_build.py to convert docs/*.md into the styled guide/ HTML.
#
# Pinned to an EXACT version on purpose: build.sh's freshness backstop compares
# the regenerated guide/ byte-for-byte against the committed output, and
# Python-Markdown's HTML can shift between releases (whitespace, attribute
# quoting, entity choices). An exact pin keeps the committed artifacts and the
# release-time regeneration in lockstep. Bump this and regenerate guide/ together.
markdown==3.9
