{
  "schema_version": "2.0",
  "praxen_version": "0.7.7",
  "scan": {
    "agent": "Deep Agents CLI",
    "agent_slug": "deepagents-cli",
    "scan_date": "2026-05-29",
    "scan_timestamp": "2026-05-29T17:40:05Z",
    "workspace": "/Users/steve.wilson/Documents/github/deckard/local/full-suite-2026-05-29/sources/deepagents-cli-src",
    "artifact_count": 22
  },
  "intro_band": {
    "agent_remit_summary": "Deep Agents CLI is a local developer command-line tool that scaffolds, bundles, and ships a `deepagents` agent for deployment via three subcommands — `init`, `dev`, and `deploy` — running the bundle on a local `langgraph dev` server or pushing it to a managed deployment platform (LangSmith/LangGraph Platform) under the developer's own credentials. Its central promise is that the bundle faithfully and only reflects the developer's reviewed project: assembled solely from declared sources, with no surface added or exposed that the developer did not configure. The remit requires an explicit operator-confirmation gate before any deploy that would leave the API open without authentication, mandates that remote MCP servers be reached over TLS and pinned to a known-good version, and forbids credentials from ever being written, committed, logged, or folded into the seeded payload.",
    "agent_structure_summary": "Python package `deepagents_cli` whose in-scope surface is the `deploy` module: `commands.py` (the `init`/`dev`/`deploy` handlers), `bundler.py` (assembles the build directory and `_seed.json`), `config.py` (parses and validates `deepagents.toml`), `templates.py` (string templates for the generated `deploy_graph.py`, `auth.py`, `pyproject.toml`, MCP loader), and `context_hub.py` (LangSmith Hub backend). `config.py:validate()` rejects unknown TOML sections and validates MCP transport, and `bundler._build_seed()` explicitly excludes `.env` from the seed payload. The anonymous-auth confirmation prompt lives in `commands._deploy()` but is guarded by a frontend-enabled condition; `_validate_mcp_for_deploy()` checks transport type but not URL scheme; no `logging` handler is ever configured; and the root `.mcp.json` declares two remote HTTP MCP servers (langchain docs/reference) with no version pin."
  },
  "behavior_summary": "The dominant pattern is *operative controls with honest, scoped gaps in the deploy path* — the bundler genuinely assembles only declared sources, skips `.env` from the seed payload, and validates config before shipping, but three guarantees the remit makes are enforced only partially. The headline gap is a conditional safety gate: the anonymous-open-API confirmation in `commands._deploy()` fires only when `[frontend].enabled`, so an anonymous-auth deploy with no frontend generates the permissive `auth.py` and ships an open API with no warning and no prompt. Alongside it, `_validate_mcp_for_deploy()` validates MCP transport type but never checks that http/sse URLs use TLS, and remote MCP servers are carried into the bundle with no version pin or integrity check — both direct divergences from Action-Boundary and Known-Good-Baseline rules. Underneath, the deploy tooling installs no logging and ships no red-team or security-disclosure artifacts, and the in-repo threat model is explicitly stale (pre-0.1.0 split).",
  "remit_coverage": {
    "stat_counts": {
      "verified": 7,
      "gap": 1,
      "partial": 6,
      "vague": 0,
      "enp": 0,
      "total": 14
    },
    "rules": [
      {
        "rule_id": "R-01",
        "section": "Action Boundaries",
        "rule_text": "The CLI MUST bundle only from the project's declared sources — its configuration, system prompt, skills, declared MCP servers, and declared subagents — and MUST NOT pull in undeclared content.",
        "status": "verified",
        "finding_id": null
      },
      {
        "rule_id": "R-02",
        "section": "Approval Requirements",
        "rule_text": "A deploy that opens the deployed API without authentication MUST be confirmed by the operator before it runs; it MUST NOT proceed unattended.",
        "status": "partial",
        "finding_id": "PRAX-2026-05-29-001"
      },
      {
        "rule_id": "R-03",
        "section": "Action Boundaries",
        "rule_text": "Remote MCP servers carried into the bundle MUST be reached over TLS, and their transport configuration MUST be validated before the bundle is shipped.",
        "status": "partial",
        "finding_id": "PRAX-2026-05-29-002"
      },
      {
        "rule_id": "R-04",
        "section": "Action Boundaries",
        "rule_text": "Project configuration MUST be validated before bundling, and a bundle that fails validation MUST NOT be deployed.",
        "status": "verified",
        "finding_id": null
      },
      {
        "rule_id": "R-05",
        "section": "Forbidden Actions",
        "rule_text": "Credentials — deployment-platform keys, model-provider API keys, hub and tracing tokens, frontend auth secrets — MUST NOT be written into the project, committed to version control, logged, or printed.",
        "status": "verified",
        "finding_id": null
      },
      {
        "rule_id": "R-06",
        "section": "Forbidden Actions",
        "rule_text": "Secret material MUST NOT be embedded into bundle artifacts that are not meant to carry it — environment files travel as environment files, never folded into the seeded-memory or skills payload.",
        "status": "verified",
        "finding_id": null
      },
      {
        "rule_id": "R-07",
        "section": "Forbidden Actions",
        "rule_text": "The CLI MUST NOT silently mutate, generate, or deploy any agent surface the developer did not declare in the project.",
        "status": "partial",
        "finding_id": "PRAX-2026-05-29-001"
      },
      {
        "rule_id": "R-08",
        "section": "Behavioral Expectations",
        "rule_text": "Before a deploy, the CLI MUST present the operator a clear summary of what the bundle contains and where it will be shipped — enough for the developer to recognise the surface being deployed.",
        "status": "verified",
        "finding_id": null
      },
      {
        "rule_id": "R-09",
        "section": "Behavioral Expectations",
        "rule_text": "A dry run MUST generate the deployment artifacts without shipping them or contacting the deployment platform's mutating endpoints.",
        "status": "partial",
        "finding_id": "PRAX-2026-05-29-006"
      },
      {
        "rule_id": "R-10",
        "section": "Escalation and Limits",
        "rule_text": "The project SHOULD publish a threat model and a security-disclosure process, and SHOULD keep the threat model current with what the package actually ships — confirming that the bundler carries only declared sources, that secrets never land in the seeded payload, and that the unauthenticated-deploy confirmation gate genuinely fires.",
        "status": "partial",
        "finding_id": "PRAX-2026-05-29-005"
      },
      {
        "rule_id": "R-11",
        "section": "Known Good Baseline",
        "rule_text": "Dependencies MUST be version-controlled with a committed, pinned lockfile, pinned to compatible ranges, and the dependency tree kept small and reviewable.",
        "status": "verified",
        "finding_id": null
      },
      {
        "rule_id": "R-12",
        "section": "Known Good Baseline",
        "rule_text": "The deployment bundle's own dependency set MUST be derived only from the project's declared model provider, declared MCP usage, declared sandbox provider, and declared auth provider — never hand-edited into the bundle out of band.",
        "status": "partial",
        "finding_id": "PRAX-2026-05-29-004"
      },
      {
        "rule_id": "R-13",
        "section": "Known Good Baseline",
        "rule_text": "Remote MCP servers declared in the project MUST be pinned to a known-good, integrity-checked version, so the deployed agent does not auto-install an unpinned server afresh.",
        "status": "gap",
        "finding_id": "PRAX-2026-05-29-003"
      },
      {
        "rule_id": "R-14",
        "section": "Behavioral Expectations",
        "rule_text": "The CLI operates under direct developer supervision as a one-shot command; it MUST NOT run as an unattended background service.",
        "status": "verified",
        "finding_id": null
      }
    ]
  },
  "findings": [
    {
      "id": "PRAX-2026-05-29-001",
      "severity": "High",
      "summary": "The anonymous-open-API confirmation gate fires only when a frontend is configured, so an anonymous-auth deploy with no frontend ships an open API silently.",
      "description": "The remit requires explicit operator confirmation before any deploy that leaves the API open without authentication. In `commands._deploy()` the `is_anonymous` guard is true only when `config.frontend is not None and config.frontend.enabled and config.auth.provider == \"anonymous\"`, yet `bundler.bundle()` generates the permissive anonymous `auth.py` whenever `auth.provider` is set regardless of frontend. A deploy with `[auth] provider = \"anonymous\"` and no `[frontend]` therefore ships an API open to anyone with the URL with no prominent warning and no confirmation prompt.",
      "tags": [
        {
          "kind": "raise",
          "label": "Implement Zero Trust"
        },
        {
          "kind": "owasp_agentic",
          "label": "ASI03 — Identity and Privilege Abuse"
        },
        {
          "kind": "owasp_llm",
          "label": "LLM06 — Excessive Agency"
        }
      ],
      "policy_rule_ids": "R-02, R-07",
      "policy_rule_text": "A deploy that opens the deployed API without authentication MUST be confirmed by the operator before it runs; it MUST NOT proceed unattended. / The CLI MUST NOT silently mutate, generate, or deploy any agent surface the developer did not declare in the project.",
      "evidence": [
        {
          "file": "libs/cli/deepagents_cli/deploy/commands.py",
          "line": 287,
          "snippet": "is_anonymous requires config.frontend.enabled — lines 287-293; anonymous auth without a frontend bypasses the warning+confirm block at 293-326 entirely."
        },
        {
          "file": "libs/cli/deepagents_cli/deploy/bundler.py",
          "line": 246,
          "snippet": "auth.py is written whenever auth_provider is not None (line 246), so the permissive AUTH_BLOCK_ANONYMOUS handler ships even when no frontend triggered the confirm."
        }
      ],
      "recommended_actions": [
        "In `commands._deploy()` compute the anonymous-API condition from `config.auth.provider == \"anonymous\"` alone (drop the `frontend.enabled` requirement) so the warning and confirmation fire for every anonymous deploy.",
        "Add a unit test asserting that an anonymous-auth, no-frontend deploy raises the confirmation prompt and aborts on a non-`y` answer."
      ],
      "raise_category": "implement_zero_trust",
      "owasp_llm": "LLM06",
      "owasp_agentic": "ASI03",
      "confidence": "High",
      "related_findings": [],
      "escalation": "alert"
    },
    {
      "id": "PRAX-2026-05-29-002",
      "severity": "High",
      "summary": "Deploy validates MCP transport type but never checks that http/sse MCP endpoints use TLS, so a plaintext http:// MCP server passes validation and ships.",
      "description": "The remit requires remote MCP servers carried into the bundle to be reached over TLS and validated before shipping. `_validate_mcp_for_deploy()` rejects only the `stdio` transport — it reads `type`/`transport` and flags stdio — but applies no scheme check to the `url`, so an `http://` (non-TLS) endpoint is accepted and copied into the bundle. The generated `_load_mcp_tools()` then connects to whatever URL is present with no TLS enforcement, exposing MCP traffic and any `${VAR}`-injected auth headers to interception.",
      "tags": [
        {
          "kind": "raise",
          "label": "Implement Zero Trust"
        },
        {
          "kind": "mcp",
          "label": "All remote connections use TLS"
        },
        {
          "kind": "owasp_agentic",
          "label": "ASI07 — Insecure Inter-Agent Communication"
        }
      ],
      "policy_rule_ids": "R-03",
      "policy_rule_text": "Remote MCP servers carried into the bundle MUST be reached over TLS, and their transport configuration MUST be validated before the bundle is shipped.",
      "evidence": [
        {
          "file": "libs/cli/deepagents_cli/deploy/config.py",
          "line": 293,
          "snippet": "_validate_mcp_for_deploy loops servers checking only `transport == \"stdio\"` (lines 293-299); no check that server_config[\"url\"] starts with https://."
        },
        {
          "file": "libs/cli/deepagents_cli/deploy/templates.py",
          "line": 446,
          "snippet": "_load_mcp_tools accepts any http/sse url via _expand(cfg[\"url\"]) (lines 446-466) with no TLS scheme guard before MultiServerMCPClient connects."
        }
      ],
      "recommended_actions": [
        "In `_validate_mcp_for_deploy()`, after expanding `${VAR}` references, reject any http/sse server whose `url` does not begin with `https://` (allowing `http://127.0.0.1`/`localhost` only if a local-loopback exception is intended).",
        "Add a unit test that a plaintext `http://` MCP url produces a validation error and aborts the deploy."
      ],
      "raise_category": "implement_zero_trust",
      "owasp_llm": null,
      "owasp_agentic": "ASI07",
      "confidence": "High",
      "related_findings": [],
      "escalation": "alert"
    },
    {
      "id": "PRAX-2026-05-29-003",
      "severity": "High",
      "summary": "Remote MCP servers are copied into the bundle by URL only, with no version pin or integrity check, so the deployed agent resolves an unpinned server at runtime.",
      "description": "The Known Good Baseline requires declared remote MCP servers to be pinned to a known-good, integrity-checked version. `bundler.bundle()` copies `mcp.json` verbatim to `_mcp.json` and `_load_mcp_tools()` connects to each `url` at deploy-graph runtime with no version, digest, or manifest check. The in-scope root `.mcp.json` declares two remote HTTP servers (`docs-langchain`, `reference-langchain`) as bare URLs, so whatever those endpoints serve at invocation time is loaded as live tools — a silent rug-pull surface.",
      "tags": [
        {
          "kind": "raise",
          "label": "Manage Your Supply Chain"
        },
        {
          "kind": "mcp",
          "label": "Tool definitions are signed and version-pinned"
        },
        {
          "kind": "owasp_agentic",
          "label": "ASI04 — Agentic Supply Chain Vulnerabilities"
        }
      ],
      "policy_rule_ids": "R-13",
      "policy_rule_text": "Remote MCP servers declared in the project MUST be pinned to a known-good, integrity-checked version, so the deployed agent does not auto-install an unpinned server afresh.",
      "evidence": [
        {
          "file": ".mcp.json",
          "line": 2,
          "snippet": "Two remote http MCP servers declared as bare urls (docs-langchain, reference-langchain) — no version, digest, or pin field."
        },
        {
          "file": "libs/cli/deepagents_cli/deploy/bundler.py",
          "line": 194,
          "snippet": "mcp.json copied verbatim to _mcp.json (lines 194-197) with no integrity or version processing before it ships in the bundle."
        }
      ],
      "recommended_actions": [
        "Require declared remote MCP servers to carry a pinned, integrity-checkable reference (e.g. a content digest or version field) and verify it in `_validate_mcp_for_deploy()` before copying into the bundle.",
        "Record the resolved MCP server identity/digest in the bundle summary so the operator sees exactly which MCP surface is being shipped."
      ],
      "raise_category": "manage_your_supply_chain",
      "owasp_llm": "LLM03",
      "owasp_agentic": "ASI04",
      "confidence": "High",
      "related_findings": [],
      "escalation": "alert"
    },
    {
      "id": "PRAX-2026-05-29-004",
      "severity": "Medium",
      "summary": "The generated bundle's inferred dependencies are emitted as bare unpinned package names with no committed bundle lockfile.",
      "description": "The Known Good Baseline requires the bundle's dependency set to be derived only from declared sources, which `_render_pyproject()` does correctly — model, MCP-adapter, sandbox, and auth deps are inferred from config, not hand-edited. But each inferred dep is written as a bare name (`langchain-anthropic`, `langchain-mcp-adapters`, `pyjwt`, sandbox partner packages) with no version constraint, and the bundle ships no lockfile, so the deployed environment resolves whatever the index serves at build time. Only `deepagents==0.5.3` and the conditional `langsmith>=0.7.35` carry pins.",
      "tags": [
        {
          "kind": "raise",
          "label": "Manage Your Supply Chain"
        },
        {
          "kind": "owasp_llm",
          "label": "LLM03 — Supply Chain"
        }
      ],
      "policy_rule_ids": "R-12",
      "policy_rule_text": "The deployment bundle's own dependency set MUST be derived only from the project's declared model provider, declared MCP usage, declared sandbox provider, and declared auth provider — never hand-edited into the bundle out of band.",
      "evidence": [
        {
          "file": "libs/cli/deepagents_cli/deploy/bundler.py",
          "line": 480,
          "snippet": "deps list built from bare provider/sandbox/auth package names (lines 480-512) with no version specifier appended."
        },
        {
          "file": "libs/cli/deepagents_cli/deploy/templates.py",
          "line": 1129,
          "snippet": "PYPROJECT_TEMPLATE pins only deepagents==0.5.3; {extra_deps} are interpolated as unpinned strings and no lockfile is generated for the bundle."
        }
      ],
      "recommended_actions": [
        "Pin each inferred dependency in `_render_pyproject()` to a tested compatible range and emit (or generate) a lockfile alongside the bundle's `pyproject.toml`."
      ],
      "raise_category": "manage_your_supply_chain",
      "owasp_llm": "LLM03",
      "owasp_agentic": null,
      "confidence": "High",
      "related_findings": [],
      "escalation": "log_only"
    },
    {
      "id": "PRAX-2026-05-29-005",
      "severity": "Medium",
      "summary": "The deploy tooling installs no logging and the bundled threat model is stale, leaving deploy actions recorded only as transient print output.",
      "description": "Across `deepagents_cli` no `logging.basicConfig`, `FileHandler`, `RotatingFileHandler`, or `dictConfig` is ever installed — modules obtain `logging.getLogger(__name__)` loggers that have no configured handler, so deploy/bundle/hub-seed progress is surfaced only through `print()`. The remit's SHOULD-keep-threat-model-current clause is also unmet: `THREAT_MODEL.md` carries a banner stating it predates the 0.1.0 split and describes the now-removed interactive REPL, not the current `deepagents_cli.deploy` surface. There is no durable, structured record of what was bundled or shipped.",
      "tags": [
        {
          "kind": "raise",
          "label": "Monitor Continuously"
        },
        {
          "kind": "owasp_llm",
          "label": "LLM06 — Excessive Agency"
        }
      ],
      "policy_rule_ids": "R-10",
      "policy_rule_text": "The project SHOULD publish a threat model and a security-disclosure process, and SHOULD keep the threat model current with what the package actually ships — confirming that the bundler carries only declared sources, that secrets never land in the seeded payload, and that the unauthenticated-deploy confirmation gate genuinely fires.",
      "evidence": [
        {
          "file": "libs/cli/deepagents_cli/deploy/commands.py",
          "line": 572,
          "snippet": "Deploy progress reported via print() (\"Deploying to LangSmith Deployments...\", lines 572-582); no logging handler is configured anywhere in deepagents_cli."
        },
        {
          "file": "libs/cli/THREAT_MODEL.md",
          "line": 5,
          "snippet": "Banner (lines 5-13) states the doc predates the 0.1.0 split and its REPL/MCP/sandbox sections no longer apply — stale versus what the package now ships."
        }
      ],
      "recommended_actions": [
        "Configure a structured logger (JSON or key-value, file or stderr handler) for the deploy path that records each bundle and deploy action with timestamp, agent name, auth mode, and destination.",
        "Regenerate `THREAT_MODEL.md` against the current `deepagents_cli.deploy` surface and add a SECURITY.md disclosure process."
      ],
      "raise_category": "monitor_continuously",
      "owasp_llm": "LLM06",
      "owasp_agentic": null,
      "confidence": "High",
      "related_findings": [],
      "escalation": "log_only"
    },
    {
      "id": "PRAX-2026-05-29-006",
      "severity": "Medium",
      "summary": "The local `dev` command seeds the remote LangSmith hub repo before starting the local server, contacting a mutating platform endpoint during a nominally local run.",
      "description": "The remit scopes `dev` to bundling and running locally and reserves platform-mutating calls for `deploy` (with dry-run explicitly forbidden from contacting mutating endpoints). `commands._dev()` calls `_seed_hub_repo()` unconditionally before launching `langgraph dev`, which performs a real `upload_files` commit to the LangSmith Hub repo when `memories.backend == \"hub\"` (the default). A developer running `dev` to test locally thus creates/writes a remote hub repo as a side effect, exceeding the local-only expectation. The deploy dry-run path is correct by contrast — it returns before `_seed_hub_repo`.",
      "tags": [
        {
          "kind": "raise",
          "label": "Implement Zero Trust"
        },
        {
          "kind": "owasp_agentic",
          "label": "ASI02 — Tool Misuse and Exploitation"
        }
      ],
      "policy_rule_ids": "R-09",
      "policy_rule_text": "A dry run MUST generate the deployment artifacts without shipping them or contacting the deployment platform's mutating endpoints.",
      "evidence": [
        {
          "file": "libs/cli/deepagents_cli/deploy/commands.py",
          "line": 415,
          "snippet": "_dev calls _seed_hub_repo(config, build_dir) (line 415) before the langgraph dev launch, with no dry-run/local-only guard."
        },
        {
          "file": "libs/cli/deepagents_cli/deploy/commands.py",
          "line": 533,
          "snippet": "_seed_hub_repo performs backend.upload_files(batch) (line 533) — a mutating commit to the remote LangSmith Hub repo, default backend=\"hub\"."
        }
      ],
      "recommended_actions": [
        "Gate `_seed_hub_repo()` in `_dev()` behind an explicit opt-in (or route `dev` to the local store backend) so a local development run does not create or mutate a remote hub repo by default."
      ],
      "raise_category": "implement_zero_trust",
      "owasp_llm": null,
      "owasp_agentic": "ASI02",
      "confidence": "Medium",
      "related_findings": [],
      "escalation": "log_only"
    },
    {
      "id": "PRAX-2026-05-29-007",
      "severity": "Medium",
      "summary": "No adversarial-testing artifact or security-scanning CI exists for the deploy package; testing is functional only.",
      "description": "The deploy package ships functional unit and integration tests (`test_bundler.py`, `test_config.py`, `test_commands.py`, `test_context_hub.py`) that assert bundle shape and config parsing, but none exercise the security-relevant edge cases — there is no test for the anonymous-no-frontend gate, no test for a plaintext MCP url, and no negative-path adversarial suite. The repo has lint/test/lockfile CI but no security-scanning workflow (no codeql/bandit/semgrep/pip-audit) and no evidence that findings drove design changes. For a tool whose output is a deployed, internet-reachable artifact, the absence of adversarial testing is a gap.",
      "tags": [
        {
          "kind": "raise",
          "label": "Build an AI Red Team"
        }
      ],
      "policy_rule_ids": null,
      "policy_rule_text": null,
      "evidence": [
        {
          "file": "libs/cli/tests/unit_tests/deploy",
          "line": null,
          "snippet": "Test suite asserts seed/config/auth-present shapes only; no test covers the anonymous-no-frontend open-API path or a non-TLS MCP url."
        },
        {
          "file": ".github/workflows",
          "line": null,
          "snippet": "Workflows cover lint/test/lockfile/version checks; no codeql/bandit/semgrep/pip-audit security-scanning job present."
        }
      ],
      "recommended_actions": [
        "Add negative-path tests for the security gates (anonymous-no-frontend confirmation, non-TLS MCP rejection, unpinned-MCP rejection) and wire a static-analysis/dependency-audit job into CI."
      ],
      "raise_category": "build_an_ai_red_team",
      "owasp_llm": null,
      "owasp_agentic": null,
      "confidence": "Medium",
      "related_findings": [],
      "escalation": "log_only"
    }
  ],
  "positives": [
    {
      "title": "Bundle assembled solely from declared project sources",
      "description": "`bundler._build_seed` and `bundle()` read only the canonical declared layout (AGENTS.md, skills/, mcp.json, user/, subagents/) and never pull in undeclared content, satisfying the CLI's core declared-sources-only guarantee.",
      "evidence_path": "libs/cli/deepagents_cli/deploy/bundler.py:328"
    },
    {
      "title": "Secrets excluded from the seeded-memory payload",
      "description": "The bundler builds `_seed.json` from memory and skills only and copies `.env` as a standalone file, so model/platform credentials never get folded into the seeded-memory or skills payload.",
      "evidence_path": "libs/cli/deepagents_cli/deploy/bundler.py:199"
    },
    {
      "title": "Config validated before any bundle is produced",
      "description": "`DeployConfig.validate` checks required files, MCP transport, sandbox/auth/memories settings, and credentials, and `_deploy`/`_dev` abort on any error before bundling — a failing config is never shipped.",
      "evidence_path": "libs/cli/deepagents_cli/deploy/config.py:204"
    },
    {
      "title": "Committed pinned lockfile and dependency-hygiene CI for the CLI",
      "description": "The CLI ships a committed `uv.lock` and the repo runs dependabot plus `check_lockfiles` and `check_sdk_pin` workflows, keeping the CLI's own dependency tree pinned and reviewable.",
      "evidence_path": "libs/cli/uv.lock:1"
    },
    {
      "title": "Pre-deploy bundle summary presented to the operator",
      "description": "`print_bundle_summary` shows the agent name, model, auth mode (explicitly flagging \"anonymous (API open to anyone)\"), seeded files, MCP presence, sandbox, and destination before a deploy proceeds.",
      "evidence_path": "libs/cli/deepagents_cli/deploy/bundler.py:520"
    }
  ],
  "log_files": {
    "present": false,
    "no_logs_note": "No logging handler is configured anywhere in `deepagents_cli` (no basicConfig/FileHandler/dictConfig); deploy progress is emitted only via print(), and the generated bundle ships no logging config — see PRAX-2026-05-29-005.",
    "rows": []
  },
  "raise_posture": {
    "weighted_overall": 2.15,
    "weighted_rationale": "Partial. Deep Agents CLI has a coherent, narrowly-scoped deploy surface with several real controls that run on the agent's path — declared-source-only bundling, a secrets-excluded seed payload, pre-bundle config validation, a committed pinned lockfile for the CLI itself, and an anonymous-deploy confirmation prompt — so it is not a hobby-grade target. But its strongest remit promises are enforced only partially: the open-API confirmation gate is conditional on a frontend being configured, MCP TLS is never checked, and the deployed bundle's own dependencies (and the remote MCP servers it carries) ship unpinned. Zero Trust and the testing/monitoring categories carry the weight: no logging is installed, there is no adversarial-testing or security-disclosure evidence, and the bundled threat model is stale.",
    "categories": [
      {
        "key": "limit_your_domain",
        "name": "Limit Your Domain",
        "score": 3,
        "confidence": "High",
        "weight": 0.15,
        "rationale": "The CLI exposes exactly three subcommands (`init`/`dev`/`deploy`) with bare invocation redirecting to the separate `deepagents-code` package, and `config._parse_config` rejects unknown TOML sections and keys, so the deploy surface is tightly and code-enforced scoped to the remit's Known Good Baseline."
      },
      {
        "key": "balance_your_knowledge_base",
        "name": "Balance Your Knowledge Base",
        "score": 3,
        "confidence": "Medium",
        "weight": 0.15,
        "rationale": "The CLI does not ingest external content into an LLM context — it bundles only the project's declared sources — and `bundler._build_seed` deliberately omits `.env` so secrets never enter the seeded memory payload; the residual concern is that declared MCP/skills content is carried verbatim and the in-repo threat model is stale."
      },
      {
        "key": "implement_zero_trust",
        "name": "Implement Zero Trust",
        "score": 2,
        "confidence": "High",
        "weight": 0.25,
        "rationale": "`config.validate` runs before any bundle and the anonymous-deploy confirmation prompt exists, but the prompt is gated on `[frontend].enabled` (so an anonymous deploy with no frontend ships an open API silently) and `_validate_mcp_for_deploy` checks transport type without ever verifying http/sse URLs use TLS — real controls with exploitable gaps."
      },
      {
        "key": "manage_your_supply_chain",
        "name": "Manage Your Supply Chain",
        "score": 3,
        "confidence": "High",
        "weight": 0.15,
        "rationale": "The CLI ships a committed `uv.lock` plus dependabot and `check_lockfiles`/`check_sdk_pin` CI, but the generated bundle's `pyproject.toml` adds model/sandbox/auth partner deps as bare unpinned names with no bundle lockfile, and remote MCP servers are carried into the bundle with no version pin or integrity check."
      },
      {
        "key": "build_an_ai_red_team",
        "name": "Build an AI Red Team",
        "score": 1,
        "confidence": "Medium",
        "weight": 0.15,
        "rationale": "Functional unit and integration tests exist for the bundler, config, and hub backend, but there is no adversarial-testing artifact, no security-scanning CI workflow (no codeql/bandit/semgrep/pip-audit), no SECURITY.md disclosure process, and the bundled `THREAT_MODEL.md` is explicitly stale (pre-0.1.0 split)."
      },
      {
        "key": "monitor_continuously",
        "name": "Monitor Continuously",
        "score": 1,
        "confidence": "High",
        "weight": 0.15,
        "rationale": "Modules obtain `logging.getLogger(__name__)` loggers but no `basicConfig`, `FileHandler`, or `dictConfig` is ever installed by the deploy tooling, and the generated bundle ships no logging configuration — deploy progress is conveyed only through `print()` statements, leaving no durable, structured action record."
      }
    ]
  },
  "footer": {
    "severity_counts": {
      "critical": 0,
      "high": 3,
      "medium": 4,
      "low": 0,
      "info": 0
    }
  }
}
