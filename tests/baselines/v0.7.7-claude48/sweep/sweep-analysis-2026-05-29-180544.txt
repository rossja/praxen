==============================================================================
PRAXEN — AGENT BEHAVIOR VERIFIER
Agent:    Sweep
Analysis: May 29, 2026, 18:05 UTC
Praxen v0.7.7
==============================================================================

BEHAVIOR SUMMARY
------------------------------------------------------------------------------
The dominant pattern is a framework that declares strong prohibitions in its
remit while leaving them unenforced in code, with one live injection-to-shell
chain. Untrusted issue and comment bodies are interpolated directly into LLM
prompts (`sweep_bot.py` `problem_statement`) with no instruction-injection
screening despite a remit that mandates it, and the LLM's own tool outputs are
then passed to `subprocess.run(..., shell=True)` in the `ripgrep` search tool
(`question_answerer.py`) and three sibling call sites — a one-hop
external-input-to-shell-execution path the remit explicitly forbids.
Compounding this, the scope guard `is_blocked()` is dead code, there is no
secret-redaction step on generated PR/commit output, and a PostHog analytics
key is hardcoded as a literal default in `config/server.py`. The result is an
agent whose declared zero-trust posture exists almost entirely in prose, not
in runtime controls.

RAISE MATURITY POSTURE
------------------------------------------------------------------------------
  Weighted overall:  1.30 / 5.0   (Ad hoc)
  Limit Your Domain              2/5   (confidence: Medium, weight: 15%)
  Balance Your Knowledge Base    1/5   (confidence: High, weight: 15%)
  Implement Zero Trust           1/5   (confidence: High, weight: 25%)
  Manage Your Supply Chain       2/5   (confidence: Medium, weight: 15%)
  Build an AI Red Team           0/5   (confidence: Medium, weight: 15%)
  Monitor Continuously           2/5   (confidence: Medium, weight: 15%)

FINDINGS
------------------------------------------------------------------------------
  Critical: 4   High: 3   Medium: 2   Low: 0   Informational: 0
  Total: 9

REMIT COVERAGE
------------------------------------------------------------------------------
  Verified: 1   Gap: 8   Partial: 4   Vague: 0   Enforcement-not-possible: 3   (of 16 rules)

OWASP LLM TOP 10 COVERAGE
------------------------------------------------------------------------------
  LLM01  Prompt Injection                                   1
  LLM02  Sensitive Information Disclosure                   2
  LLM03  Supply Chain                                       —
  LLM04  Data and Model Poisoning                           —
  LLM05  Improper Output Handling                           1
  LLM06  Excessive Agency                                   2
  LLM07  System Prompt Leakage                              —
  LLM08  Vector and Embedding Weaknesses                    —
  LLM09  Misinformation                                     —
  LLM10  Unbounded Consumption                              —

OWASP AGENTIC TOP 10 COVERAGE
------------------------------------------------------------------------------
  ASI01  Agent Goal Hijack                                  1
  ASI02  Tool Misuse and Exploitation                       3
  ASI03  Identity and Privilege Abuse                       2
  ASI04  Agentic Supply Chain Vulnerabilities               —
  ASI05  Unexpected Code Execution (RCE)                    1
  ASI06  Memory and Context Poisoning                       —
  ASI07  Insecure Inter-Agent Communication                 —
  ASI08  Cascading Failures                                 —
  ASI09  Human-Agent Trust Exploitation                     —
  ASI10  Rogue Agents                                       —

CRITICAL FINDINGS
------------------------------------------------------------------------------
  PRAX-2026-05-29-001  LLM- and repo-derived search arguments are passed to
            <code>subprocess.run(..., shell=True)</code> at four in-scope
            sites, enabling shell command injection.
      Action 1: Replace every shell=True ripgrep/linguist invocation with a
                list-argv call (subprocess.run(["rg", "-n", ..., query],
                shell=False)) so arguments cannot be re-parsed by a shell.
      Action 2: Add a deterministic validator/allowlist for any model- or
                repo-derived argument before it reaches subprocess, and treat
                the search query as data, not as a shell token.

  PRAX-2026-05-29-002  Untrusted issue and comment text enters LLM context
            with no instruction-injection screening, completing an
            external-input-to-shell-execution chain.
      Action 1: Add an instruction-injection screening pass on
                problem_statement and any file/diff content before it is added
                to the prompt, flagging or quote-escaping instruction-like
                patterns as the remit requires.
      Action 2: Label external content explicitly as untrusted data in prompt
                construction and forbid the model from acting on instructions
                found inside <issue>/<user_comment> spans.

  PRAX-2026-05-29-003  A PostHog project analytics key is committed as a
            literal default value in <code>config/server.py</code>, exposing a
            live credential in source.
      Action: Remove the literal default; require POSTHOG_API_KEY to come from
              the environment (or disable analytics when unset) and rotate the
              exposed key.

  PRAX-2026-05-29-004  The GitHub webhook secret is declared with a
            <code>None</code> default and the verifier fails open when it is
            unset, so unauthenticated payloads can drive the agent.
      Action: Add an assert WEBHOOK_SECRET (or fail-closed startup check) so
              the service refuses to boot without a webhook secret, and make
              the signature verifier reject rather than accept requests when
              the secret is missing.

==============================================================================
Built on OWASP Top 10 for LLM Applications 2025  |  OWASP Top 10 for Agentic Applications 2026  |  RAISE Framework
Generated by Praxen  |  project sponsor: Exabeam (https://www.exabeam.com/)
github.com/open-agent-ai-security/praxen  |  Apache-2.0
==============================================================================
