==============================================================================
PRAXEN — AGENT BEHAVIOR VERIFIER
Agent:    AutoGen Code Execution Subsystem
Analysis: May 29, 2026, 19:04 UTC
Praxen v0.7.7
==============================================================================

BEHAVIOR SUMMARY
------------------------------------------------------------------------------
The dominant pattern is *sandbox with the shape of isolation but not the
substance*: every isolation guarantee the remit demands is either defaulted
off, downgraded silently, or never built. LocalCommandLineCodeExecutor copies
the parent process's entire os.environ into the subprocess and gates nothing —
it merely emits a warnings.warn — while its own docstring claims a
dangerous-command regex denylist that does not exist anywhere in the code.
DockerCommandLineCodeExecutor creates containers with no user, read_only,
mem_limit, cap_drop, or network restriction, so model-generated code runs as
root with full capabilities and outbound network;
create_default_code_executor() silently downgrades that already-thin Docker
path to the local executor on a mere UserWarning. No executor writes a
per-execution audit record, so none of these gaps is detectable after the
fact.

RAISE MATURITY POSTURE
------------------------------------------------------------------------------
  Weighted overall:  1.45 / 5.0   (Ad hoc)
  Limit Your Domain              3/5   (confidence: High, weight: 15%)
  Balance Your Knowledge Base    2/5   (confidence: Medium, weight: 15%)
  Implement Zero Trust           1/5   (confidence: High, weight: 25%)
  Manage Your Supply Chain       2/5   (confidence: Medium, weight: 15%)
  Build an AI Red Team           1/5   (confidence: Medium, weight: 15%)
  Monitor Continuously           0/5   (confidence: High, weight: 15%)

FINDINGS
------------------------------------------------------------------------------
  Critical: 4   High: 5   Medium: 3   Low: 0   Informational: 0
  Total: 12

REMIT COVERAGE
------------------------------------------------------------------------------
  Verified: 4   Gap: 8   Partial: 4   Vague: 0   Enforcement-not-possible: 2   (of 18 rules)

OWASP LLM TOP 10 COVERAGE
------------------------------------------------------------------------------
  LLM01  Prompt Injection                                   —
  LLM02  Sensitive Information Disclosure                   2
  LLM03  Supply Chain                                       1
  LLM04  Data and Model Poisoning                           —
  LLM05  Improper Output Handling                           —
  LLM06  Excessive Agency                                   1
  LLM07  System Prompt Leakage                              —
  LLM08  Vector and Embedding Weaknesses                    —
  LLM09  Misinformation                                     —
  LLM10  Unbounded Consumption                              2

OWASP AGENTIC TOP 10 COVERAGE
------------------------------------------------------------------------------
  ASI01  Agent Goal Hijack                                  —
  ASI02  Tool Misuse and Exploitation                       2
  ASI03  Identity and Privilege Abuse                       3
  ASI04  Agentic Supply Chain Vulnerabilities               —
  ASI05  Unexpected Code Execution (RCE)                    3
  ASI06  Memory and Context Poisoning                       —
  ASI07  Insecure Inter-Agent Communication                 —
  ASI08  Cascading Failures                                 —
  ASI09  Human-Agent Trust Exploitation                     —
  ASI10  Rogue Agents                                       1

CRITICAL FINDINGS
------------------------------------------------------------------------------
  PRAX-2026-05-29-001  LocalCommandLineCodeExecutor copies the full parent
            os.environ into the subprocess and gates execution with only a
            warning, not host isolation.
      Action 1: In _execute_code_dont_check_setup, build the subprocess env
                from an explicit allow-list (PATH plus virtualenv additions)
                instead of os.environ.copy(), so host credentials are never
                inherited by executed code.
      Action 2: Add an explicit opt-in flag (e.g. allow_host_env=False
                default) and require the operator application to set it before
                any host-environment passthrough occurs.

  PRAX-2026-05-29-002  No executor records a per-execution audit log, leaving
            every code execution and every isolation gap undetectable after
            the fact.
      Action: Add a structured audit-log emission at the top of each
              executor's execute_code_blocks recording timestamp, executor
              class, language, configured work_dir, timeout, and a SHA-256
              digest of the code (not the body), and a second entry with
              exit_code on completion.

  PRAX-2026-05-29-003  DockerCommandLineCodeExecutor creates containers with
            no user, read_only, cap_drop, or mem_limit, so model code runs as
            root with full capabilities.
      Action 1: In containers.create, set user="1000:1000" (or a non-root UID
                baked into the image), read_only=True with a writable
                /workspace volume, cap_drop=["ALL"], and
                security_opt=["no-new-privileges"] as the defaults.
      Action 2: Add mem_limit and pids_limit defaults and surface them as
                constructor parameters governed by the resource-ceiling
                approval rule.

  PRAX-2026-05-29-004  Docker containers are created with default networking
            and no egress control, letting model code exfiltrate data or pull
            payloads with no approval.
      Action 1: Default network_mode="none" on containers.create and require
                an explicit, operator-approved allow-list parameter to enable
                any egress, satisfying the network-approval rule.
      Action 2: Replace publish_all_ports=True with binding only the single
                kernel-gateway port to 127.0.0.1.

==============================================================================
Built on OWASP Top 10 for LLM Applications 2025  |  OWASP Top 10 for Agentic Applications 2026  |  RAISE Framework
Generated by Praxen  |  project sponsor: Exabeam (https://www.exabeam.com/)
github.com/open-agent-ai-security/praxen  |  Apache-2.0
==============================================================================
